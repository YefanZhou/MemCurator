"""Vendored copy of sea-mem-policy `curator/` memory library (verbatim).

Do NOT edit files under this package — they are byte-identical copies of the
original so the transferred curator method stays faithful (and matches any
trained curator checkpoint's expected prompt format). See PROVENANCE.md.

The `curator_v1_alfworld.py` adapter in the parent dir wraps these classes into
the runner's sync retrieve()/add() seam (BM25 retrieval, ALFWorld).
"""
